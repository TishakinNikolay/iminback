/**
 * FilterComparisonOperator enums
 */
export enum FilterComparisonOperatorEnum {
    _gt = '_gt',
    _lt = '_lt',
    _gte = '_gte',
    _lte = '_lte',
    _eq = '_eq',
    _neq = '_neq',
    _in = '_in',
    _nin = '_nin'
}
